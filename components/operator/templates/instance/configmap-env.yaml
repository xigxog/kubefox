apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ name }}-env
  namespace: {{ namespace }}
  labels:
    {{- include "labels" . | nindent 4 }}
  {{- with .Owner }}
  ownerReferences:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
data:
  KUBEFOX_HOME: {{ homePath }}
  KUBEFOX_INSTANCE_NAMESPACE: {{ .Instance.Namespace }}
  KUBEFOX_INSTANCE: {{ .Instance.Name }}
  KUBEFOX_NAMESPACE: {{ namespace }}
  {{- with .Platform.LogFormat }}
  KUBEFOX_LOG_FORMAT: {{ . }}
  {{- end }}
  {{- with .Platform.LogLevel }}
  KUBEFOX_LOG_LEVEL: {{ . }}
  {{- end }}